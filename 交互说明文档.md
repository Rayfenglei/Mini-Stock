# FinX Stock 交互说明文档 v2.0

## 概述

本文档详细描述 FinX Stock 小程序各页面的交互逻辑、用户操作流程和状态变化。

---

## 1. 全局交互

### 1.1 页面导航
- **Tab 切换**：底部导航栏，3个主入口
  - 首页：资产概览和持仓列表
  - 分析：数据分析和图表
  - 我的：个人中心和设置
- **页面跳转**：
  - 普通跳转：wx.navigateTo
  - 返回：wx.navigateBack
  - Tab切换：wx.switchTab

### 1.2 通用交互
- **下拉刷新**：页面顶部下拉刷新数据
- **上拉加载**：列表底部上拉加载更多
- **点击反馈**：所有可点击元素有 active 状态
- **加载状态**：数据加载时显示 loading

### 1.3 手势操作
- **左滑删除**：持仓列表左滑显示删除按钮
- **长按**：部分元素支持长按操作
- **双指缩放**：图表支持双指缩放

---

## 2. 首页交互

### 2.1 页面结构
```
首页
├── 顶部欢迎区
│   ├── 用户问候
│   └── 通知按钮
├── 总资产卡片（渐变）
│   ├── 时间选择器
│   ├── 总资产金额
│   ├── 收益变化
│   └── 迷你趋势图
├── 快捷操作区
│   ├── 添加资产
│   ├── 资产分析
│   ├── 交易记录
│   └── 设置
├── 投资组合标题
│   ├── 标题和数量
│   └── 排序按钮
├── 持仓列表
│   └── 可滑动卡片
└── 浮动添加按钮
```

### 2.2 交互流程

#### 总资产卡片
1. **时间选择器点击**
   - 触发：点击时间选择器
   - 动作：弹出时间范围选择器（本周/本月/本年/全部）
   - 反馈：选中项高亮，卡片数据更新

2. **趋势图**
   - 触发：页面加载完成
   - 动作：绘制迷你趋势图
   - 反馈：动画展示曲线

#### 快捷操作区
1. **添加资产**
   - 触发：点击"+"图标或浮动按钮
   - 动作：跳转到添加资产页面
   - 反馈：按钮缩放动画

2. **其他快捷入口**
   - 触发：点击对应图标
   - 动作：跳转到对应页面
   - 反馈：图标颜色变化

#### 持仓列表
1. **卡片点击**
   - 触发：点击持仓卡片
   - 动作：跳转到资产详情页
   - 反馈：卡片轻微缩放

2. **左滑删除**
   - 触发：左滑卡片超过60rpx
   - 动作：显示删除按钮
   - 继续左滑：直接删除
   - 点击删除：弹出确认对话框
   - 右滑/点击其他：恢复原位

3. **排序切换**
   - 触发：点击排序按钮
   - 动作：切换排序方式（市值/盈亏/名称）
   - 反馈：按钮文字变化，列表重新排序

### 2.3 状态变化
- **空状态**：无持仓时显示空状态插图和添加按钮
- **加载状态**：数据加载时显示骨架屏
- **错误状态**：网络错误时显示重试按钮

---

## 3. 资产详情页交互

### 3.1 页面结构
```
资产详情页
├── 顶部导航
│   ├── 返回按钮
│   ├── 资产名称和代码
│   └── 更多按钮
├── 资产类型标签栏
├── 价格卡片
│   ├── 当前价格
│   ├── 涨跌幅
│   └── 价格统计（今开/最高/最低/昨收）
├── 持仓概览卡片
│   ├── 持仓市值
│   ├── 总盈亏
│   └── 详细数据网格
├── 走势图卡片
│   ├── 时间选项卡
│   └── 价格走势图
├── 交易记录卡片
│   ├── 标题和查看全部
│   └── 最近交易列表
└── 底部操作栏
    ├── 卖出按钮
    └── 买入按钮
```

### 3.2 交互流程

#### 顶部导航
1. **返回按钮**
   - 触发：点击返回
   - 动作：返回上一页
   - 反馈：按钮高亮

2. **更多按钮**
   - 触发：点击更多
   - 动作：弹出操作菜单（编辑/删除/分享）
   - 反馈：菜单从顶部滑入

#### 价格卡片
1. **价格刷新**
   - 触发：下拉页面
   - 动作：刷新实时价格
   - 反馈：价格数字闪烁更新

#### 走势图
1. **时间切换**
   - 触发：点击时间选项
   - 动作：切换时间范围（1日/1周/1月/1年）
   - 反馈：选项高亮，图表重绘

2. **图表交互**
   - 触发：触摸图表
   - 动作：显示十字准线和具体数值
   - 反馈：tooltip 弹出

#### 交易记录
1. **查看全部**
   - 触发：点击"查看全部"
   - 动作：跳转到交易记录页面
   - 反馈：页面过渡动画

2. **单条记录**
   - 触发：点击交易记录
   - 动作：显示交易详情
   - 反馈：底部弹窗

#### 底部操作
1. **买入/卖出**
   - 触发：点击对应按钮
   - 动作：跳转到交易页面
   - 反馈：按钮缩放，页面过渡

---

## 4. 个人中心页交互

### 4.1 页面结构
```
个人中心页
├── 用户信息卡片（渐变）
│   ├── 用户头像
│   ├── 用户名和ID
│   ├── 设置按钮
│   └── 统计概览（持仓/交易/天数）
├── 资产管理分区
│   ├── 我的账户
│   ├── 交易记录
│   └── 智能提醒
├── 数据分析分区
│   ├── 资产分析
│   ├── 盈亏统计
│   └── 导出报表
├── 系统设置分区
│   ├── 数据刷新设置
│   ├── 涨跌颜色设置
│   └── 通知设置
└── 关于与帮助分区
    ├── 使用帮助
    ├── 意见反馈
    └── 关于我们
```

### 4.2 交互流程

#### 用户信息卡片
1. **头像点击**
   - 触发：点击头像
   - 动作：弹出头像选择（拍照/相册）
   - 反馈：底部弹窗

2. **设置按钮**
   - 触发：点击设置
   - 动作：跳转到设置页面
   - 反馈：按钮旋转动画

#### 功能菜单
1. **菜单项点击**
   - 触发：点击任意菜单项
   - 动作：跳转到对应页面
   - 反馈：
     - 背景色变化
     - 箭头位移动画
     - 页面过渡

2. **带徽章的菜单**
   - 触发：点击带数字徽章的菜单
   - 动作：跳转到对应页面
   - 反馈：徽章消失动画

#### 统计数字
1. **数字点击**
   - 触发：点击统计数字
   - 动作：跳转到对应详情页
   - 反馈：数字放大动画

---

## 5. 分析页交互

### 5.1 页面结构
```
分析页
├── 页面标题
├── 时间选择器
├── 收益概览卡片（渐变）
├── 关键指标网格（2x2）
│   ├── 最佳持仓
│   ├── 最差持仓
│   ├── 今日收益
│   └── 持仓数量
├── 资产分布图表
├── 资产趋势图表
├── 持仓排行
└── 风险提示（条件显示）
```

### 5.2 交互流程

#### 时间选择器
1. **时间切换**
   - 触发：点击时间选项
   - 动作：切换时间范围，刷新所有数据
   - 反馈：
     - 选中项渐变背景
     - 数据卡片更新动画
     - 图表重绘

#### 关键指标卡片
1. **卡片点击**
   - 触发：点击指标卡片
   - 动作：跳转到对应详情
   - 反馈：卡片上浮效果

#### 图表交互
1. **资产分布图**
   - 触发：点击图例
   - 动作：显示/隐藏对应数据
   - 反馈：图例透明度变化

2. **趋势图**
   - 触发：点击切换按钮（市值/盈亏）
   - 动作：切换图表数据
   - 反馈：按钮高亮，图表过渡动画

#### 持仓排行
1. **排序切换**
   - 触发：点击排序选项
   - 动作：切换排序方式
   - 反馈：列表重新排序动画

2. **排行项点击**
   - 触发：点击排行项
   - 动作：跳转到资产详情
   - 反馈：项高亮

---

## 6. 添加资产页交互

### 6.1 页面结构
```
添加资产页
├── 顶部导航
├── 资产类型选择
│   ├── 股票
│   ├── 基金
│   └── 黄金
├── 搜索框
├── 搜索结果列表
└── 手动添加表单
    ├── 资产代码
    ├── 资产名称
    ├── 成本价格
    ├── 持有数量
    └── 确认按钮
```

### 6.2 交互流程

#### 资产类型选择
1. **类型切换**
   - 触发：点击类型选项
   - 动作：切换资产类型
   - 反馈：选项高亮，搜索提示变化

#### 搜索功能
1. **输入搜索**
   - 触发：输入资产代码/名称
   - 动作：实时搜索匹配
   - 反馈：
     - 输入时显示清除按钮
     - 显示搜索结果列表
     - 无结果显示提示

2. **结果选择**
   - 触发：点击搜索结果
   - 动作：自动填充表单
   - 反馈：结果项高亮，表单数据更新

#### 表单提交
1. **表单验证**
   - 触发：点击确认按钮
   - 动作：验证表单数据
   - 反馈：
     - 验证通过：提交数据
     - 验证失败：显示错误提示

2. **提交成功**
   - 触发：数据提交成功
   - 动作：返回首页
   - 反馈：成功提示 toast

---

## 7. 交易页面交互

### 7.1 页面结构
```
交易页面
├── 顶部导航
├── 资产信息
├── 交易类型选择（买入/卖出）
├── 价格输入
├── 数量输入
├── 金额计算
├── 交易日期
└── 确认按钮
```

### 7.2 交互流程

#### 交易类型
1. **类型切换**
   - 触发：点击买入/卖出
   - 动作：切换交易类型
   - 反馈：选中项高亮，界面颜色变化（买入红/卖出绿）

#### 价格输入
1. **价格选择**
   - 触发：点击价格输入框
   - 动作：弹出数字键盘
   - 反馈：输入框聚焦

2. **快捷价格**
   - 触发：点击快捷价格按钮
   - 动作：自动填充价格
   - 反馈：按钮高亮

#### 数量输入
1. **数量调整**
   - 触发：使用加减按钮
   - 动作：增减数量
   - 反馈：数字变化动画

2. **全部卖出**
   - 触发：点击"全部"按钮
   - 动作：填充全部可卖数量
   - 反馈：按钮高亮

#### 金额计算
- 实时计算：价格 × 数量
- 显示预估金额
- 显示手续费（如有）

---

## 8. 动画规范

### 8.1 页面过渡动画
```css
/* 页面进入 */
@keyframes pageEnter {
  from {
    opacity: 0;
    transform: translateY(20rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
/* 时长：300ms，缓动：ease-out */
```

### 8.2 元素动画
```css
/* 按钮点击 */
.btn:active {
  transform: scale(0.98);
  transition: transform 0.2s ease;
}

/* 卡片滑动 */
.card.animating {
  transition: transform 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

/* 进度条 */
.progress-fill {
  transition: width 0.3s ease;
}

/* 淡入 */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
/* 时长：300ms */
```

### 8.3 加载动画
```css
/* 骨架屏脉冲 */
@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}
/* 时长：2000ms，循环 */
```

---

## 9. 错误处理

### 9.1 网络错误
- **提示方式**：Toast 提示
- **提示内容**："网络连接失败，请检查网络"
- **重试机制**：显示重试按钮

### 9.2 数据错误
- **提示方式**：页面内提示
- **提示内容**：具体错误信息
- **处理方式**：提供返回或重试选项

### 9.3 操作错误
- **提示方式**：表单内错误提示
- **提示内容**：字段验证错误
- **处理方式**：高亮错误字段，显示错误信息

---

## 10. 性能优化

### 10.1 列表优化
- 使用虚拟列表（长列表）
- 图片懒加载
- 分页加载数据

### 10.2 渲染优化
- 避免频繁 setData
- 使用 CSS 动画代替 JS 动画
- 合理使用 will-change

### 10.3 网络优化
- 数据缓存
- 请求合并
- 增量更新

---

## 附录

### A. 交互检查清单
- [ ] 所有可点击元素有反馈
- [ ] 页面过渡流畅
- [ ] 加载状态明确
- [ ] 错误提示友好
- [ ] 手势操作顺畅

### B. 手势定义
| 手势 | 触发区域 | 动作 |
|------|----------|------|
| 点击 | 按钮、列表项 | 跳转/操作 |
| 长按 | 资产卡片 | 弹出菜单 |
| 左滑 | 持仓列表 | 显示删除 |
| 下拉 | 页面顶部 | 刷新数据 |
| 上拉 | 列表底部 | 加载更多 |

### C. 更新日志
- v2.0 (2026-04-10): 全新交互设计，优化用户体验
- v1.0: 初始版本

---

**文档维护**：产品团队
**最后更新**：2026-04-10
**版本**：v2.0
